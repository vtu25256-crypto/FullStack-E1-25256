// ============================================================
// config.js — Application configuration (main branch)
// ============================================================

module.exports = {
    port: 3000,
    currency: "USD",
    maxWithdrawalLimit: 10000,
    interestRates: {
        savings: 3.5,
        fixed:   6.0
    }
};
